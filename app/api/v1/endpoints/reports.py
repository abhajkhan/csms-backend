"""Reports endpoints — placeholder.

Implementation pending Phase 7 — Reporting.

Planned endpoints (03_API_CONTRACT.md §8):
    GET  /reports/attendance  — attendance report
    GET  /reports/expenses    — expense breakdown by site
    GET  /reports/salary      — worker salary summary
    GET  /reports/wallet      — supervisor wallet history
    GET  /reports/warehouse   — warehouse stock report
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement reporting endpoints.
