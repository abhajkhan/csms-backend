"""Purchases endpoints — placeholder.

Implementation pending Phase 5 — Normal Driver Purchase Management.

Planned endpoints (03_API_CONTRACT.md §8):
    POST  /purchases       — record a purchase (normal driver)
    GET   /purchases       — list purchases
    GET   /purchases/{id}  — get purchase details
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement purchase endpoints.
