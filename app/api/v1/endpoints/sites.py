"""Sites endpoints — placeholder.

Implementation pending Phase 1 — Site Management.

Planned endpoints (03_API_CONTRACT.md §8):
    POST   /sites       — create site (admin only)
    GET    /sites       — list sites
    GET    /sites/{id}  — get site details
    PATCH  /sites/{id}  — update site
    DELETE /sites/{id}  — archive site
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement site management endpoints.
