"""Expenses endpoints — placeholder.

Implementation pending Phase 4 — Expense Management.

Planned endpoints (03_API_CONTRACT.md §8):
    POST  /expenses       — record a site expense (supervisor)
    GET   /expenses       — list expenses
    GET   /expenses/{id}  — get expense details
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement expense endpoints.
