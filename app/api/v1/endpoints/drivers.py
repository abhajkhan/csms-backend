"""Driver log endpoints — placeholder.

Implementation pending Phase 5 — Driver Operations.

Planned endpoints (03_API_CONTRACT.md §8):
    POST  /ajax-logs     — submit Ajax driver daily log
    GET   /ajax-logs     — list Ajax driver logs

    POST  /hitachi-logs  — submit Hitachi driver daily log
    GET   /hitachi-logs  — list Hitachi driver logs

    POST  /worker-payments  — record worker advance or settlement
    GET   /worker-payments  — list worker payments
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement driver log and worker payment endpoints.
