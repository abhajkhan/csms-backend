"""Attendance endpoints — placeholder.

Implementation pending Phase 3 — Attendance Management.

Planned endpoints (03_API_CONTRACT.md §8):
    POST   /attendance       — mark attendance record
    GET    /attendance       — list attendance records
    PATCH  /attendance/{id}  — update attendance (admin verification)
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement attendance endpoints.
