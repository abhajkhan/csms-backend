"""Workers endpoints — placeholder.

Implementation pending Phase 2 — Worker Management.

Planned endpoints (03_API_CONTRACT.md §8):
    POST   /workers       — register worker (admin or supervisor)
    GET    /workers       — list workers
    GET    /workers/{id}  — get worker details
    PATCH  /workers/{id}  — update worker
    DELETE /workers/{id}  — deactivate worker (soft delete)
"""

from fastapi import APIRouter

router = APIRouter()

# TODO: Implement worker management endpoints.
