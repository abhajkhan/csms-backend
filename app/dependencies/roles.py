"""Role-based access control dependencies — placeholder.

Implementation pending Phase 1 — Auth & User Management.

TODO: Implement role guard factories, e.g.:
    require_admin = RoleChecker(allowed_roles=["admin"])
    require_supervisor = RoleChecker(allowed_roles=["admin", "supervisor"])
"""
