"""Domain enums — placeholder.

Implementation pending Phase 1.

TODO: Define Python Enum classes matching the database enum columns
      from Construction_System_Design_v2.md.

Enums to define:
    UserRole         — admin, supervisor, driver
    DriverType       — hitachi, ajax, normal
    SiteStatus       — active, completed, on_hold
    AttendanceStatus — present, absent, half_day
    LabourType       — USER, WORKER
    ExpenseType      — material_transfer, cash_purchase, driver_material,
                       driver_bata, driver_vehicle_rent, ajax_service,
                       hitachi_service, misc
    ReferenceType    — stock_movement, purchase, ajax_log, hitachi_log
    MovementType     — IN, OUT
    PaymentType      — advance, settlement
    VehicleType      — own, outer, none
    TxnType          — credit, debit
    DestinationType  — site, warehouse
    ItemCategory     — cement, metal, sand, aggregate, wood, other
"""
